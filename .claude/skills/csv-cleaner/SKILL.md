---
name: csv-cleaner
description: Use when the user wants to clean CSV data and generate charts.
---

# CSV Cleaner + Chart

1. Read the CSV file and analyze structure.
2. Clean data:
   - Remove empty rows and columns
   - Trim whitespace from all cells
   - Fix data types (numbers, dates)
   - Remove duplicates
   - Handle missing values (fill or remove)
3. Generate summary statistics:
   - Row/column counts
   - Data types per column
   - Missing value counts
   - Min/max/mean for numeric columns
4. Create charts (if requested):
   - Bar chart for categorical data
   - Line chart for time series
   - Scatter plot for correlations
   - Histogram for distributions
5. Export cleaned data and charts.
